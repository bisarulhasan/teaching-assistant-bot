# Base URL of the teaching-assistant backend (FastAPI on Render in prod).
NEXT_PUBLIC_API_BASE_URL=https://noor-xets.onrender.com

# Supabase Auth (public client keys — safe in the browser).
NEXT_PUBLIC_SUPABASE_URL=https://<project>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon or publishable key>
